#pragma once
#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

std::string TakeUserString();
bool CheckPalindromeValidity(std::string& inputString);
std::string CreatePalindrome(std::string inputString);
bool isProgramStillRunning();